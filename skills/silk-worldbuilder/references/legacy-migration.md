# Legacy SILK migration

Treat `novel_world/worlds/<world>` as source material, not proof of completion.

1. Preserve the old package unchanged.
2. Create the new world template at a separate path.
3. Derive stable subject IDs from actual identity, not old category paths.
4. Group fragmented identity information into one canonical subject file.
5. Promote an old origin, event, mechanism, or doctrine to a separate subject only when it has independent identity or multi-subject consequences.
6. Convert old `links` into reviewed relation proposals; reject co-location and category-only links.
7. Rebuild dynamic collections as views. Do not copy `world_shape.json` as authoritative taxonomy.
8. Reopen every old approved node as pending migration material.
9. Reset subject maturity, world level, and clean audit streak.
10. Record old path, new stable ID, new canonical path, status decision, and reason in the decision log.

Do not carry old self-scores, universal frontmatter, minimum-link rules, or phase completion forward.

